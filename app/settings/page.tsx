import { SettingsView } from "@/components/SettingsView";

export const metadata = {
  title: "Settings — Digital Quran",
  description: "Set your reading preferences for the Digital Quran.",
};

export default function SettingsPage() {
  return <SettingsView />;
}